server:
  port: 8081
spring:
  application:
    name: embedded-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/BarathArivazhagan/config-server-repository.git
        bootstrap: true

